{
  "name": "live-translate-api-spike",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "smoke": "node --env-file-if-exists=.env scripts/qwen-smoke.mjs"
  },
  "dependencies": {
    "ws": "^8.18.3"
  }
}
